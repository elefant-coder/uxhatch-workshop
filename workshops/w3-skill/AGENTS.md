# W3 Codex 手順書

CodexはこのAGENTS.mdの手順書を読んで実行してください。

金庫を開けるには、手がかりファイルを次の順で読んでください。

1. `vault/clue_c.txt`
2. `vault/clue_a.txt`
3. `vault/clue_b.txt`

順番はファイル名順ではありません。必ず C → A → B です。

読んだ3つの値をハイフン `-` で連結し、`answer.txt` に1行で書きます。前後空白や末尾改行は入れないでください。
